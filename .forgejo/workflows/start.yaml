on: [push]
jobs:
  test:
    runs-on: docker
    container:
      image: ubuntu:latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - run: |
          ls -la
          echo "test"

